import 'package:flutter/material.dart';

class FolderInfo extends StatelessWidget {
  const FolderInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("info"),
    );
  }
}
